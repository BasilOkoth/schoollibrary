# This app uses the existing backup models in digitallibrary.models.
# Do not define TenantBackup or TenantRestoreLog here, because they
# already exist in digitallibrary and duplicate models break Django checks.
