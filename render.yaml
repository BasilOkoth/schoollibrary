services:
  - type: web
    name: schoollibrary
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn schoollibrary.wsgi:application --bind 0.0.0.0:10000
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: schoollibrary_db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: false
      - key: ALLOWED_HOSTS
        value: .onrender.com
      - key: PYTHON_VERSION
        value: 3.11.0
      # ✅ THIS IS THE MISSING LINE
      - key: DJANGO_SETTINGS_MODULE
        value: schoollibrary.settings
    # ✅ ADD A HEALTH CHECK PATH
    healthCheckPath: /healthz/

databases:
  - name: schoollibrary_db
    plan: free
