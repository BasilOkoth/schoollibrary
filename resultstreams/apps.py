from django.apps import AppConfig


class ResultStreamsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "resultstreams"
    verbose_name = "Teacher Result Assignments"
