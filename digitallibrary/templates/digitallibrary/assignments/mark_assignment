{% extends "digitallibrary/base.html" %}
{% block title %}Mark Assignment{% endblock %}
{% block content %}
<div class="max-w-3xl mx-auto px-4 py-8">
    <h1 class="text-2xl font-bold text-white mb-2">Mark Assignment</h1>
    <p class="text-gray-400 mb-6">Review the submitted work and return feedback.</p>

    <div class="bg-gray-800 rounded-xl border border-gray-700 p-5 mb-6">
        <h2 class="text-lg font-bold text-white">{{ submission.resource.title }}</h2>
        <p class="text-gray-400 text-sm mt-1">Student: {{ submission.student.first_name }} {{ submission.student.last_name }} - {{ submission.student.admission_number }}</p>
        {% if submission.student_note %}<div class="mt-4 bg-gray-900 rounded-lg border border-gray-700 p-3"><p class="text-gray-500 text-xs uppercase">Student Note</p><p class="text-gray-300 text-sm mt-1">{{ submission.student_note }}</p></div>{% endif %}
        <a href="{{ submission.submitted_file.url }}" target="_blank" class="inline-block mt-4 text-blue-400 hover:text-blue-300">Open Submitted Work</a>
    </div>

    <form method="post" class="bg-gray-800 rounded-xl border border-gray-700 p-5">
        {% csrf_token %}
        <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mb-4"><div><label class="block text-sm text-gray-300 mb-1">Score</label>{{ form.score }}</div><div><label class="block text-sm text-gray-300 mb-1">Out of</label>{{ form.max_score }}</div></div>
        <div class="mb-4"><label class="block text-sm text-gray-300 mb-1">Teacher Comment</label>{{ form.teacher_comment }}</div>
        <div class="mb-4"><label class="block text-sm text-gray-300 mb-1">Status</label>{{ form.status }}</div>
        <button type="submit" class="w-full bg-emerald-600 hover:bg-emerald-500 text-white py-3 rounded-lg font-semibold">Save Marking</button>
    </form>
</div>
{% endblock %}
